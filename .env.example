# Environment Variables Template
# Copy this file to .env.local, .env.development, .env.uat, or .env.production
# and fill in the appropriate values for each environment

# Database Connection String
# Format: mongodb://[username:password@]host[:port][/database][?options]
# Local example: mongodb://localhost:27017/doctor-appointments
# Atlas example: mongodb+srv://username:password@cluster.mongodb.net/database
MONGODB_URI=mongodb://localhost:27017/doctor-appointments

# NextAuth Configuration
# Generate a random secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-key-minimum-32-characters-long
NEXTAUTH_URL=http://localhost:3000

# JWT Configuration
# Generate a random secret: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-key-minimum-32-characters-long

# Node Environment
NODE_ENV=development
